{% if (clickstack_api_key | default('') | trim | length) > 0 %}
HYPERDX_API_KEY="{{ clickstack_api_key }}"
{% endif %}
{% if (clickstack_frontend_url | default('') | trim | length) > 0 %}
FRONTEND_URL="{{ clickstack_frontend_url }}"
{% endif %}
CLICKSTACK_APP_PORT="{{ clickstack_app_port }}"
CLICKSTACK_BIND_HOST="{{ clickstack_bind_host }}"
